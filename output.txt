Implemented the backend pieces from the attached review.

Changed:



publicChatService.js (line 514)


Human handoff now sends SMS to eligible support users, not just the assigned user.

Filters through the existing routing/candidate logic and respects SMS preference + valid phone presence.

Prevents duplicate sends by reusing an existing attempted notificationStatus.

Persists notificationStatus and notificationTimeline at:

session root

support

support.routing




Added public/customer-safe transcript sending with email matching, visitor/session check, rate limiting, and receipt/audit persistence.





publicChatController.js (line 93)


Added public transcript controller.





publicChatRoutes.js (line 31)


Added transcript endpoints:

POST /chat/public/session/:sessionId/transcript

POST /api/chat/public/session/:sessionId/transcript

POST /public/chat/sessions/:sessionId/transcript

POST /api/public/chat/sessions/:sessionId/transcript








supportGlobalModel.test.js (line 3084)


Added coverage for SMS handoff notification persistence and one-send behavior.

Added coverage for public transcript email safety and repeat-send rate limiting.





Verification passed:


node --check on touched service/controller/route files

vitest -t '33ca'

vitest -t '33cb'

vitest -t 'public website chat human request'

vitest -t 'public website chat timeout'


One small deployment note: SMS messages include a direct support-session link when one of these env vars is set: SUPPORT_CONSOLE_URL, WORKSIDE_SUPPORT_CONSOLE_URL, or FRONTEND_URL. If none is set, the SMS falls back to “Open Support Console to accept the transfer.”